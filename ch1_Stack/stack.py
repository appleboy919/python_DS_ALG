### Note ###
# DS in which all insertion and deletions are made at one end, called "top of the stack"
# LIFO
# Common operation: push, pop, peek, is_empty
# Application: reverse Polish notation for evaluating arithmetic expressions, syntax parsing, cold stack(space for para and local vars is created), used in recursion, undo, redo operations, low-level assembly programming
